# CAP-SAT rebuttal 证据快照 · 2026-09-10

本文是人工核对的阶段记录。最新行数和数值见面板首页。

## A47–A48 输出头与教师上限诊断 · 2026-09-18

A47 六固定模型完成拟合，21024 前后记录与完整网络重放审计无错误；
平衡模型验证权重 RMSE 再降低4.63%–8.42%，相位完整路径零翻转。相对 A46
原损失模型总降35.26%–40.65%，仍未达预定50%门，不启动它的全成本晋级。
固定ridge线性修正机制判定mixed，不是一般表示容量上界。0新增求解。

A48另行冻结192训练公式/48生成流，384新预测和768教师/度数配对求解。
原运行在97块/194单元处触发根文件系统压力；恢复等待192MiB准入后补齐
287块/574单元，未放宽128MiB运行下限。全部768单元、384预测、192参考、
实际发送哈希和恢复绑定审计无错误。公式级oracle为3.090957秒/197解出，
相对最佳固定策略仅8.3435%，低于预定10%互补门；整流四折n-only规模规则
为3.258717秒/186解出，较度数改善3.3689%、较教师3.8439%，n=200/300/350
各规模无PAR-2或解出数伤害，因此只通过规模规则门。双门失败，
`may_propose_scale_route=false`。两者都是排除选择器费用的离线重放，不能称
部署或CAP增量；`end_to_end_advantage=false`、`independently_confirmed=false`，
独立留出仍封存。正向信号保留为后续假设，但停止扩大分类器；原先七个缺点仍
0完全解决/4部分改善/3未解决。

## A49 稀疏 rank-blend 全成本试验 · 2026-09-19

为绕开继续拟合教师的死路，冻结了“度数为主、只在最大权重分歧的10%变量上
用教师/度数几何中点纠偏”的单一规则。使用与A48流分离的96个新TRAIN公式，
192次教师预测、96个CaDiCaL参考和576个三臂全成本单元完成；预测、变换、
序列化、求解、实际发送哈希和完整SAT检查均计费并审计无误。v1在首个求解
单元前因runner变量错误失败，保留且不计科学结果；v2仅修复该工程缺陷后
重新冻结。

|策略|PAR-2 / 192单元|解出|
|---|---:|---:|
|最小度数|3.341495|90|
|教师|3.504405|84|
|10%稀疏纠偏|3.403518|88|

稀疏纠偏较最强度数慢1.856%，n=300为4.652342秒/16解，对度数
4.506495秒/18解；n=200为0.156935/64，对度数0.137247/64；n=350为
5.401277/8，对度数5.380742/8。预检中变换/序列化中位数约2.35–3.91ms，
低于教师约23ms预测费，因此不能把失败归咎于简单算术开销。该机制门失败，
停止同一静态权重插值家族；无留出、无独立确认、不是端到端CAP优势。下一
个可行方向若继续，须改变求解器交互并预先加入在线回退成本。

## A40–A46 方法修复追加 · 2026-09-18

A40 的 12 模型、110592 更新和 28512 评估记录通过审计，教师动作一致率
99.61%–99.75%；固定选择 d6_raw 全部三种子，不挑最快种子。A43 的 432 次
完整单例预测显示中位数 18.02→10.05–11.26ms；不是单纯内核计时或求解结论。

A44 在 144 未用于调参的开发公式上完成 2016 全成本单元和 1152 新预测。
三学生比默认快 12.9%–15.1%，但比教师慢 0.56%–3.21%、比最小度数慢
4.04%–6.78%，严格性能门均失败。实际发送 payload 没有独立哈希回执；本轮
动作到求解输入仅由冻结代码路径核对，不能扩写成历史输入回执重建。

A45 在同批已暴露开发上另跑 3168 诊断单元、逐条保存实际发送边界哈希：
替换教师权重恢复三个学生额外搜索成本的 97.7%/99.7%/89.6%，只换教师相位
几乎没有改善。所有预测费用排除，只是理想动作修复的机制诊断，不是部署优势。

A46 据此改变训练损失尺度：训练数据确定权重系数 53.8409449，对比原系数 1；
全部六模型 18432 更新、14256 记录通过审计。各规模权重误差下降 31%–39%，
相位质量守住，但未达预定减半门槛。已定位并部分缓解权重精度问题，没有新的
端到端性能通过。训练与验证误差接近，裁剪极少；下一步仅计划冻结表示的
权重输出头精修诊断，尚未运行。144 独立留出仍封存，七缺口未整体解决，论文不改。

## 当前结论

计时管道缺陷已有修复与重跑证据；随机 SAT 的 stock 性能差距和工业迁移尚未解决。完成更多实验没有自动消除这些负结果。

| 实验 | 最新已核对状态 | 结论边界 |
|---|---|---|
| E17 n350 | 五种子，1500 行 | 第五种子包含跨主机补跑 |
| E19S | 五种子，1500 行 | SAT 子集相对 stock 的 PAR-2 收益约 -63.7 秒 |
| E19L | 五种子，1500 行 | SAT 子集相对 stock 的 PAR-2 收益约 -47.9 秒 |
| E17 n400 | seed 42，300 行 | stock 141/141 SAT，CAP 83/141 |
| E18 | 19 唯一应用实例 | 五个 CaDiCaL 检查点及 Kissat seed 789 完成，无正向迁移证据 |
| E20 | 六臂，1800 求解任务 | 支持原生相位机制与覆盖接口相互作用；不支持简单等价归因 |
| E21 | 设计文档记录负向探索 | 持久行级证据未恢复，v23 不引用数值 |

PAR-2 收益定义为对照减 CAP，正值有利于 CAP。E17/E19 共有 4500 个四臂实例行；不要与 4500 次单臂求解混淆。当前并集标签为 SAT 155、UNSAT 128、未决 17。

## 论文状态

v23 的本地审计记录 E16–E20 冻结时完整单元已纳入，六个新表通过复算与字节核对，PDF 为 60 页、正文和完整结论在前 9 页。该审计冻结时只有四个完整 E17 CaDiCaL 种子，E19L 仍不完整；最新完成数据尚未纳入。

这是一份本地一致性审计，不是独立复现实验或接收预测。NeuroBack 原生对比仍待跟进。作者、OpenReview 与 AI 声明需要负责人提供事实信息。

## 统计与来源限制

面板区间只重采样种子，并条件于固定 CNF 面板；不同运行的资源竞争和混合主机补跑限制因果解释。SAT 标签来自所有求解器确定性输出并集，不是所有实例的已知总体标签。

补充敏感性检查：对三组统一排除 seed 1024，保留相同的 155 个 SAT 标签，使用其余四个种子的平均对照减 CAP PAR-2。Frozen 收益 -81.3 秒，95% 种子区间 [-88.1, -74.6]；small -65.5 [-76.4, -55.3]；large -47.1 [-57.5, -36.5]。排除涉及补跑的种子后，随机 SAT 的 stock 差距依然存在。该检查不消除其余运行间的资源竞争，也没有替代生成簇层面的不确定性分析。

证据来源：CAP-SAT 项目内的 EXPERIMENT_DESIGN_NEXTGEN_20260825、sprint_e19_multiscale_readout_v1、sprint_20260910/CLAIM_AUDIT、REMOTE_AGENTS v23 handoff。面板公开汇总，不发布原始 CNF、模型或内部运行信息。

## 协作审计追加 · 2026-09-10

写作 agent 已确认与实验 / 面板侧的职责分工。规范协作入口为 iclr_2027/REMOTE_AGENTS；原生任务由原 owner 负责训练、交接和恢复，面板只读取监督快照。

新的 e19_followup_20260910_v1 快照冻结 485 个来源文件，复核 4500 个四臂实例行、准备文件、检查点与实际 CNF 哈希。对 5 个训练种子和 10 个生成簇做 20,000 次交叉重采样，保留同实例的对照配对。标签仍条件于固定求解结论并集；属于事后描述性统计，未做多重比较校正，不作因果训练收益解释。

| 模型 | SAT 对 stock PAR-2 收益 / 秒 | 95% 种子 × 生成簇联合区间 |
|---|---|---|
| Frozen | -73.4 | [-118.3, -33.8] |
| E19S | -63.7 | [-101.5, -30.4] |
| E19L | -47.9 | [-71.9, -24.3] |
| E19L，统一排除 seed 1024 | -47.1 | [-70.8, -24.8] |

这补充了统计核验，但未解决相对原生求解器的性能缺口。面板首页的区间仍明确标为种子单独区间，与此处联合区间不可混用。新审计通过本地复算与一致性检查，未取得不同模型族的独立诚信审计认证。

原生基线的监督已补阶段区分、预算与产物验收；NeuroBack 标签准备为 300 条，其中 294 条有 backbone、6 条无 backbone、0 条超时。标签准备成本单独记录，精确关键路径墙钟耗时未保存，不能用工作进程耗时之和冒充。桥接意外丢失会触发高优先级告警，仍由原任务 owner 恢复，未实现自动接管。

论文落地更新：v25 已纳入本次完整 E19 结果与联合区间；正文保持 9 页，新表位于第 39 页，本地 TeX 与旧表一致性检查通过。另核对 2800 个训练/验证 CNF 与 300 个评测 CNF 无哈希重叠，训练完成日志、凭据和检查点来源已核验。v23/v24 PDF 及其冻结审计保留。这不代表原生 RLAF/NeuroBack 的正式比较已完成。

## E60 同图同核启动复测 · 2026-09-15

128张既有E59开发图、3个种子、3轮均衡顺序重复、3种启动路径，共3,456次。
保留同一CPU、相同求解器与策略，直接继承CPU亲和性使冷启动全流程均值
17.802→12.921 ms（27.4%）；按图聚类、三比较同时95%收益区间为
[4.558,5.212] ms。计入额外Python独立校验后为19.772→14.843 ms。
每臂1152/1152成功，候选/冲突/赋值零差异，复查3456个CPU mask及2835份SAT见证。

这是已实现实验启动器的工程改进，不是新图确认、学习优势或上线服务。
后续廉价对照也必须获得同样的启动优化。首页读取与审计回执绑定的汇总，
不发布CPU编号、进程信息、绝对路径或原始公式。原始来源为E60/run的
FROZEN、RESULTS和AUDIT；面板保留摘要哈希。

## A31 共享 GPU 联合成本反馈 · 2026-09-16

已完成冻结的一小时上限队列（实际22分44秒）：96 个新训练公式、48 个验证公式、72 个开发
公式；三个优化种子，27,648 个训练反馈单元和 1,584 个开发计时单元。
固定 CAP 编码器，比较同规模的廉价结构头和 CAP 特征头；相位与变量权重
联合更新。训练采用批次分摊预测费用，开发另测单例预测，完整 SAT 校验计费。
模型设置和预热单报，因此不是冷生命周期性能试验。

首页仅导出白名单聚合进度。运行需匹配 Linux 进程创建时间和脚本路径，
心跳超过两分钟即陈旧；终态需匹配 FINAL 和完整退出记录。队列完成不等于
性能通过，仍须逐条审计和强对照门。廉价控制目前计算了多余的邻域特征，
不事后扣除估计时间；即使边缘通过，也需精简实现的实际复测才能讨论晋级。
缺失终态或清理异常不算完成。本轮完整30,384条记录重放通过，13个后期子进程
身份及4个进程组终态检查无遗留。六个候选均未通过性能门：CAP三个种子
PAR-2为3.380664/3.336710/3.295746秒，原生RLAF为3.290074秒；最接近点
估计仍慢0.17%，且n200/n300退化，不能称追平。n350约91%的训练采样组
没有有效成本差异。训练batch-share及廉价inline计时没有独立回执；不能将
一致性重放扩写为独立计时复算。运行采集器不自行判定性能；性能结论由
审计后的A31计划卡维护。七项性能缺口仍为零项全面解决。

更正：E57仅n24单轮且漏计退出，不能证明约3请求回本；E56/E59的跨图集
37%差异不能归因于取消绑核；此前分层路由仅为提议，并未完成服务切换。

## A32：新训练切片上的局部动作与预算辨别

24个未按求解结果筛选的新公式（n200/300/350各8，6生成流）；2,448单元是
34动作×3预算×24公式，不是独立确认样本。相同RLAF mode下比较全变量、
随机8/16和CAP分歧排名16变量的配对扰动；CAP付两个模型的推理费。
0.75/3/6秒预算复用相同动作；不修改与最小度数规则作为控制。完整信用
从原始判词、退出码、时限、SAT完整见证/UNSAT独立参考重推，未解保留。
H1动作收益与H2反馈信息门同时满足才考虑新训练；H3另要求CAP胜过同支持
大小的随机对照。均是小训练切片描述门，不是显著性或端到端确认。
面板显示运行快照，不展示未完成样本排名；监督结束不自动接续扩训。

A32终态：2448条完整原始记录审计通过，约6分钟完成；938观察身份/4组扫描
无遗留。H1通过：随机16对全变量3秒均值改善24.71%，但仍比不修改RLAF慢
7.39%。H2失败：困难无信用组62.5%→50%，仅改善12.5个百分点；3→6秒仅多
1/128个成功动作而反馈耗时总和增81.64%。H3失败：6秒CAP16比随机16慢7.67%。
不扩训、不生成确认，不称端到端优势。初次汇总误解析Python虚拟环境软链，
引入不同NumPy版本的微小浮点差异；原环境全量精确重放通过，失败日志保留，
没有编辑实验结果或降低审计精度。下一候选为单模型强策略蒸馏/求解偏好微调，
尚未执行，必须另行冻结协议；原论文保持不变。
